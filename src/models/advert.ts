export class Advert {
    title: string;
    description: string;
    price: number;
    country: string;
    videoUrl: string;
    created: number;
    county: string;
    id?: number;
    state?: string;
    user?: any; //to be more strongly typed later
    city?: string;
}
//todo eventually sync this up with the rest endpoints types
